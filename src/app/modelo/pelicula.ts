export class Pel {
    codigo: number; // Contiene la frase
    mensaje: string;
    pelicula:Pelicula;
    
}
export class Pelicula {
    codigo: number; // Contiene la frase
    titulo: string;
    descripcion: string;
    precio: number;
}
